{
  "name": "Angel One Ops - Final Order PDF",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "aot-final-order-pdf",
        "responseMode": "lastNode",
        "responseData": "firstEntryJson",
        "options": {}
      },
      "id": "aot-final-order-webhook",
      "name": "Receive Final Order PDF",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        0
      ],
      "webhookId": "aot-final-order-pdf"
    },
    {
      "parameters": {
        "jsCode": "const vars = typeof $vars !== \"undefined\" ? $vars : {};\nconst readConfig = (name, fallback = \"\") => vars[name] || fallback;\n\nconst expectedSecret = readConfig(\"AOT_FINAL_ORDER_WEBHOOK_SECRET\");\nconst telegramToken = readConfig(\"TELEGRAM_BOT_TOKEN\");\nconst telegramChatId = readConfig(\"TELEGRAM_CHAT_ACCOUNTANT\") || readConfig(\"TELEGRAM_CHAT_ADMIN\");\n\nconst input = items[0]?.json || {};\nconst headers = input.headers || input.request?.headers || {};\nconst body = input.body && typeof input.body === \"object\" ? input.body : input;\nconst receivedSecret = headers[\"x-aot-webhook-secret\"] || headers[\"X-Aot-Webhook-Secret\"] || headers[\"X-AOT-WEBHOOK-SECRET\"];\n\nif (expectedSecret && receivedSecret !== expectedSecret) {\n  throw new Error(\"Sai x-aot-webhook-secret\");\n}\n\nif (body.delivery?.schema !== \"aot_final_dispatch_order_pdf_v1\") {\n  throw new Error(\"Sai schema payload PDF\");\n}\n\nconst orderNo = body.order_no || \"UNKNOWN\";\nconst filename = body.delivery?.filename || `Lenh_dieu_xe_${orderNo}.pdf`;\nconst routeLegs = Array.isArray(body.trip?.route_legs) ? body.trip.route_legs : [];\nconst routeText = routeLegs.length\n  ? routeLegs.map((leg, index) => `Chặng ${index + 1}: ${leg.from || \"-\"} -> ${leg.to || \"-\"}${leg.time ? \" / \" + leg.time : \"\"}`).join(\"\\n\")\n  : [body.trip?.pickup, body.trip?.dropoff].filter(Boolean).join(\" -> \");\n\nconst text = [\n  \"📄 <b>Yêu cầu xuất lệnh điều xe final</b>\",\n  \"\",\n  `Mã lệnh: <b>${orderNo}</b>`,\n  body.customer?.name ? `Khách: ${body.customer.name}${body.customer.phone ? \" / \" + body.customer.phone : \"\"}` : \"\",\n  body.vehicle?.driver_name ? `Tài xế: ${body.vehicle.driver_name}${body.vehicle.driver_phone ? \" / \" + body.vehicle.driver_phone : \"\"}` : \"\",\n  body.vehicle?.plate ? `Xe: ${body.vehicle.plate}` : \"\",\n  routeText ? `Hành trình:\\n${routeText}` : \"\",\n  body.trip?.total ? `Tổng thanh toán: ${body.trip.total}` : \"\",\n  body.reconciliation?.reconciliation_status ? `Trạng thái hồ sơ: ${body.reconciliation.reconciliation_status}` : \"\",\n  \"\",\n  \"Workflow đã nhận payload. Bước tạo PDF cần server n8n có script Python và logo để nối tiếp.\"\n].filter(Boolean).join(\"\\n\");\n\nif (telegramToken && telegramChatId) {\n  await this.helpers.httpRequest({\n    method: \"POST\",\n    url: `https://api.telegram.org/bot${telegramToken}/sendMessage`,\n    headers: { \"Content-Type\": \"application/json\" },\n    body: {\n      chat_id: telegramChatId,\n      text,\n      parse_mode: \"HTML\",\n      disable_web_page_preview: true\n    },\n    json: true\n  });\n}\n\nreturn [{\n  json: {\n    ok: true,\n    stage: \"webhook_received\",\n    order_no: orderNo,\n    filename,\n    telegram_sent: Boolean(telegramToken && telegramChatId),\n    next_required: \"Để gửi PDF thật: upload scripts/aot_lenh_dieu_xe_pdf_generator.py và logo lên server n8n, sau đó nối Execute Command + Send Document.\"\n  }\n}];"
      },
      "id": "aot-final-order-receive-and-notify",
      "name": "Validate Payload and Notify Telegram",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        280,
        0
      ]
    }
  ],
  "connections": {
    "Receive Final Order PDF": {
      "main": [
        [
          {
            "node": "Validate Payload and Notify Telegram",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "pinData": {},
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": null,
  "tags": [],
  "triggerCount": 1,
  "updatedAt": "2026-08-31T00:00:00.000Z",
  "versionId": "angel-one-final-order-pdf-v1"
}
